import { createClient } from "@/lib/supabase/server";
export default async function EventPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const s=await createClient();const {data:e}=await s.schema("sportsbook").from("events").select("id,name,starts_at,status,is_live,markets(id,name,status,selections(id,name,status,odds_quotes(decimal_odds,expires_at,captured_at)))").eq("id",id).maybeSingle();return <main className="min-h-screen p-5"><div className="mx-auto max-w-4xl"><a href="/" className="text-sm text-slate-400">← Markets</a>{!e?<div className="panel mt-6">Event not found.</div>:<><div className="mt-6"><h1 className="text-3xl font-black">{e.name}</h1><p className="mt-2 text-slate-400">{new Date(e.starts_at).toLocaleString()} · {e.status}</p></div><div className="mt-6 space-y-4">{e.markets?.map((m:any)=><div className="panel" key={m.id}><h2 className="font-bold">{m.name}</h2><div className="mt-3 grid gap-2 sm:grid-cols-2">{m.selections?.map((p:any)=><div className="odd" key={p.id}><span>{p.name}</span><b>{Number(p.odds_quotes?.[0]?.decimal_odds||0).toFixed(2)}</b></div>)}</div></div>)}</div></>}</div></main>}