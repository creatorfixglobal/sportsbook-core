import { createClient } from "@/lib/supabase/server";
export default async function Admin(){const s=await createClient();const {data:{user}}=await s.auth.getUser();let role=null;if(user){const r=await s.schema("sportsbook").from("admin_roles").select("role,active").eq("user_id",user.id).maybeSingle();role=r.data}if(!user||!role?.active)return <main className="min-h-screen grid place-items-center p-6"><div className="panel max-w-md text-center"><h1 className="text-2xl font-bold">Admin</h1><p className="mt-2 text-slate-400">Authorized staff only.</p></div></main>;return <main className="min-h-screen p-5"><div className="mx-auto max-w-7xl"><h1 className="text-3xl font-bold">Operations</h1><p className="mt-2 text-slate-400">Role: {role.role}</p><div className="mt-6 grid gap-4 md:grid-cols-3">{["Events & Markets","Bets & Risk","Settlements","Users","Wallet Ledger","Audit Trail"].map(x=><div className="panel" key={x}><b>{x}</b><p className="mt-2 text-sm text-slate-400">Management module ready for privileged workflows.</p></div>)}</div></div></main>}
