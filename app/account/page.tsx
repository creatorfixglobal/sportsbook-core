import { createClient } from "@/lib/supabase/server";
export default async function Account(){const s=await createClient();const {data:{user}}=await s.auth.getUser();return <main className="min-h-screen p-5"><div className="mx-auto max-w-3xl"><a href="/" className="text-sm text-slate-400">← Sportsbook</a><h1 className="mt-6 text-3xl font-bold">Account</h1><div className="panel mt-5">{user?<><div className="text-sm text-slate-400">Signed in as</div><div className="mt-1 font-semibold">{user.email}</div><div className="mt-5 flex gap-3"><a className="primary" href="/wallet">Wallet</a><a className="primary" href="/my-bets">My Bets</a></div></>:<p className="text-slate-300">You are not signed in. <a className="underline" href="/login">Sign in</a></p>}</div></div></main>}