import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
export default async function Account(){
 const s=await createClient(); const {data:{user}}=await s.auth.getUser();
 return <main className="min-h-screen px-4 pb-28 pt-5 sm:px-6"><div className="mx-auto max-w-4xl"><Link href="/" className="text-sm font-semibold text-slate-400 hover:text-white">← Sportsbook</Link><div className="mt-7"><div className="text-[10px] font-black tracking-[.3em] text-emerald-400/80">ACCOUNT</div><h1 className="mt-1 text-3xl font-black">Profile & Security</h1></div>{!user?<div className="panel mt-6">You are not signed in. <Link className="underline" href="/login">Sign in</Link></div>:<div className="mt-6 grid gap-4 md:grid-cols-2"><div className="panel"><div className="text-xs font-bold uppercase tracking-wider text-slate-600">Signed in as</div><div className="mt-2 break-all font-semibold">{user.email}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><Link className="primary" href="/wallet">Wallet</Link><Link className="primary" href="/my-bets">My Bets</Link></div></div><div className="panel"><div className="font-bold">Responsible play</div><p className="mt-2 text-sm text-slate-500">Account limits, age/KYC checks, self-exclusion and jurisdiction controls belong in the compliance layer before real-money activation.</p></div><div className="panel md:col-span-2"><div className="font-bold">Security</div><div className="mt-3 grid gap-2 sm:grid-cols-3"><div className="rounded-xl border border-white/8 p-3 text-sm text-slate-400">Server-side bet validation</div><div className="rounded-xl border border-white/8 p-3 text-sm text-slate-400">Idempotent bet placement</div><div className="rounded-xl border border-white/8 p-3 text-sm text-slate-400">Protected wallet ledger</div></div></div></div>}</div></main>}
