import Link from "next/link";
import { createClient } from "@/lib/supabase/server";
export default async function Account(){const s=await createClient();const {data:{user}}=await s.auth.getUser();let profile:any=null;if(user){const r=await s.schema("sportsbook").from("user_profiles").select("display_name,status,created_at").eq("user_id",user.id).maybeSingle();profile=r.data}
return <main className="min-h-screen px-4 pb-28 pt-5 sm:px-6"><div className="mx-auto max-w-4xl"><div className="flex items-center justify-between"><Link href="/" className="text-sm font-semibold text-slate-400 hover:text-white">← Sportsbook</Link>{user?<Link href="/wallet" className="text-sm font-semibold text-slate-400 hover:text-white">Wallet →</Link>:null}</div><div className="mt-7"><div className="text-[10px] font-black tracking-[.3em] text-emerald-400/80">ACCOUNT</div><h1 className="mt-1 text-3xl font-black">Account center</h1></div>{!user?<div className="panel mt-6">You are not signed in. <Link className="underline" href="/login">Sign in</Link></div>:<><div className="panel mt-6"><div className="flex flex-wrap items-start justify-between gap-4"><div><div className="text-xs text-slate-600">Signed in as</div><div className="mt-1 font-bold">{user.email}</div>{profile?.display_name?<div className="mt-1 text-sm text-slate-500">{profile.display_name}</div>:null}</div><span className="rounded-full border border-emerald-400/20 bg-emerald-400/5 px-3 py-1 text-[10px] font-black uppercase tracking-wider text-emerald-300">{profile?.status||"active"}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><Link href="/wallet" className="rounded-xl border border-white/10 bg-white/[.025] p-4 hover:border-emerald-400/25"><b>Wallet</b><p className="mt-1 text-xs text-slate-600">Balance & ledger</p></Link><Link href="/my-bets" className="rounded-xl border border-white/10 bg-white/[.025] p-4 hover:border-emerald-400/25"><b>My Bets</b><p className="mt-1 text-xs text-slate-600">Bets & outcomes</p></Link><div className="rounded-xl border border-white/10 bg-white/[.025] p-4"><b>Security</b><p className="mt-1 text-xs text-slate-600">Session controls are handled by secure authentication.</p></div></div></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="panel"><b>Responsible play</b><p className="mt-2 text-sm text-slate-500">Limits, self-exclusion, age/KYC and jurisdiction controls belong in the compliance layer before real-money activation.</p></div><div className="panel"><b>Real-money mode</b><p className="mt-2 text-sm text-slate-500">This interface does not activate deposits or withdrawals by itself. Provider, licensing and compliance gates remain server-side.</p></div></div></>}</div></main>}
